# -¿Cuántas clases tiene el proyecto?

    R// El proyecto tiene tres clases.

#- ¿En cuál clase se define el programa principal

    R// Se define en Zoologico

#- ¿Cuántos objetos de la clase Animal se crean en la clase
principal?

    R// Se crean dos objetos de la clase animal en la clase principal zoologico.

#- ¿Cuántos y cuáles son los atributos de la clase Animal?

    R// En la clase animal tenemos cuatro atributos y son:

         - String nombre;
         - String genero;
         - double peso =1;
         - Animal pareja;

#- ¿Cuáles atributos de la clase Animal no son primitivos?

    R// Los atributos que no son primitivos en la clase animal son:

        - String nombre;
        - String genero;
        - Animal pareja;

#- ¿Qué pasa si eliminamos la línea 17 de la clase Zoologico?

     R// No se asigna ninguna pareja.

#- ¿Cuántos métodos tiene la clase Familia?

    R// En mi caso tengo 11 metodos para la clase familia. Esto incluye los constructores, los de get y set y los ordinarios.

#- ¿Cuántos parámetros tiene el método tenerHijo() de la clase
Familia?

    R// Tiene un parametro que es String nombre.

#- ¿Cuántos atributos inicializa el constructor de la clase Familia?

    R// Lo hacen 3 atributos.

#- ¿Qué tipo de retorno tiene el método tenerHijo()?

Void, siempre en la misma clase.
